# 로컬 개발용 예시 — 복사하여 .env 로 사용 (.env 는 git 미추적)
DATABASE_URL=postgresql://USER@localhost:5432/stonex_dev

# 최초 SUPER_ADMIN 시드 (기획서 §13.1 — 하드코딩 금지, 환경 변수로만)
SEED_SUPER_ADMIN_EMAIL=admin@example.com
SEED_SUPER_ADMIN_PASSWORD=change-me-on-first-login

# JWT 서명 키 (32자 이상 필수 — 미설정 시 서버 기동 실패)
JWT_SECRET=change-me-to-a-random-32char-plus-secret

# 권한 스냅샷 캐시 (미설정 시 DB 폴백으로 동작)
REDIS_URL=redis://localhost:6379

# 오브젝트 스토리지 (개발: MinIO / 운영: AWS S3 — endpoint 미지정 시 S3)
STORAGE_ENDPOINT=http://127.0.0.1:9000
STORAGE_BUCKET=stonex
STORAGE_ACCESS_KEY=change-me
STORAGE_SECRET_KEY=change-me
STORAGE_REGION=us-east-1

# 브라우저에서 API 를 호출할 출처. 콤마로 여러 개. 와일드카드는 쓰지 않는다.
CORS_ORIGINS=http://localhost:3002

# 개발 전용: 메일 본문(토큰 원문)을 서버 로그에 남긴다. 운영에서는 절대 켜지 말 것.
DEV_MAIL_LOG_BODY=0

# ── 메일 발송 (§13.2 결정: 개발 단계는 Gmail SMTP) ──
# console = 로그만 남김(기본) / smtp = 실제 발송
MAIL_TRANSPORT=console
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=보내는주소@gmail.com
# Gmail 은 **앱 비밀번호**를 발급받아 넣는다 (계정 비밀번호로는 인증되지 않음)
SMTP_PASSWORD=change-me
SMTP_FROM=stonex <보내는주소@gmail.com>

# ── 탈퇴 회원 개인정보 파기 (§13.2 결정: 30일 후 익명화) ──
# 계정 행은 남기고 식별 정보만 지운다 — 감사 로그의 행위자 추적을 보존하기 위해서다.
MEMBER_ANONYMIZE_AFTER_DAYS=30
MEMBER_ANONYMIZE_BATCH=200

# ── 부트스트랩 3종 (이 셋만 환경 변수, 나머지 운영 설정은 관리 화면에서 주입) ──
# 설정 비밀값 암호화 키. `openssl rand -base64 32` 로 생성한다.
# **DB 와 함께 유출되면 의미가 없으므로 별도로 보관한다.**
SETTINGS_ENCRYPTION_KEY=change-me-base64-32bytes
