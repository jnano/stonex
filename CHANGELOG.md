# 변경 이력

이 파일은 [Keep a Changelog](https://keepachangelog.com/ko/1.0.0/) 형식을 따르며,
버전은 [Semantic Versioning](https://semver.org/lang/ko/)을 따른다.

**major 판정 기준**(기획서 v1.7 §16.3-4): 기존 핵심 테이블의 **구조 변경**만 major 로 본다.
신규 기능 테이블 추가는 minor 다 — Phase 2 가 테이블 5종을 더하고도 `0.2.0` 인 이유다.

이 파일은 관리자 콘솔의 **버전 화면이 읽는 단일 출처**다. 화면에 따로 목록을 박아 두지 않는다
(같은 사실이 두 곳에 있으면 언젠가 갈라진다 — §15.1).

## [Unreleased]

### Added
- **리소스 타입 레지스트리** (Phase 3 WP-K1) — 커널이 리소스 타입을 코드 분기 없이
  서술자 데이터로만 안다. 평가기 상태 게이트·리소스 로더·Grant 잠금 테이블이 전부
  레지스트리를 참조하며, 신규 타입은 서술자 등록만으로 편입된다(§9.1).
  서술자는 등록 시 식별자 검증 + 부팅 시 실제 스키마 대조를 통과해야 한다(RT-26)
- G-6-lite 커널 보호 파일 게이트 — "커널 0줄 편입" 주장을 CI가 diff 로 검증(RT-35).
  의도된 커널 변경은 `kernel-change` 라벨로 명시 승인
- **게시판 사용성 개선** (Phase 3 WP-B6) — 글·댓글 수정/삭제 화면, 댓글 반응(별도 표),
  답글 펼침/접기 토글, 운영 행위(고정·숨김/해제·이동 — 삭제 아닌 되돌릴 수 있는 표시
  변경), 본문 이미지 인라인 표시·첨부 다운로드 링크(서명 URL), 작성자명·조회수 표시
- **게시판 특화·거버넌스** (Phase 3 WP-B5 — 게시판 모듈 완성) — Board Type 프리셋
  5종(공지·QNA·갤러리·포럼·FAQ — 코드 재배포 없이 설정+기능모듈 조합), 접근개입
  4종(비밀글·공동작성·차단·멘션 — 정책 훅으로만 편입, 커널·게시판 코어 무수정),
  신고·블라인드(자동 발동은 임시 숨김까지 — 삭제는 운영자 명시 결정), BRI-1~4
  모듈 순찰(고아 첨부·카운터 자동 보정), 게시판 관리 콘솔 화면
- **게시판 검색·성능** (Phase 3 WP-B4) — 게시판 내 한국어 부분일치 검색(pg_trgm
  어댑터 — GD-3 의 pg_bigm 은 로컬·CI 환경 부재로 같은 취지의 내장 contrib 채택,
  어댑터 교체 지점으로 국소화), 검색은 목록과 동일 게이트·행 조건(R-B8 누출 차단),
  조회수(요청 경로 DB 쓰기 없음 — 인메모리 버퍼 주기 플러시), 검색 화면
- **게시판 상호작용** (Phase 3 WP-B3) — 이벤트 버스(outbox, at-least-once)와
  notification 기반 모듈(팬아웃 상한·멱등 중복 억제), reaction·tag 기능모듈(게시판별
  on/off — 코어 무수정 BINV-2), 키셋 페이징(§8.2), 댓글 동시성 정밀화(부모 단위
  advisory lock)·표시 깊이 접기, 댓글 작성·반응·알림 화면
- **게시판 콘텐츠** (Phase 3 WP-B2) — 마크다운 파이프라인(서버 렌더+새니타이즈,
  표시는 body_html 만·프론트 직접 렌더는 G-2 정적 차단), 드래그앤드랍 첨부(코어
  업로드 세션 재사용, 콜백은 upload_id 만), 이미지 보안(EXIF 제거·재인코딩·형식
  이중검증·해상도 상한), 글 작성 화면
- **게시판 코어** (Phase 3 WP-B1) — 게시판·게시글·댓글(Board/Post/Comment).
  §9.1 "커널 0줄 편입"의 첫 신규 사용자: 서술자·훅·시드·골든 등록만으로 편입되고
  보호 커널 파일 diff 0 을 G-6-lite 가 증명. 2평면 접근(평가기 + canAccessBoard 정책),
  회원 전용(DEC-4), 게시판 목록·글 목록·글 읽기 화면 포함
- **거버넌스 파라미터화** (Phase 3 WP-K3) — RI-4/5/8 의 타입별 하드코딩을 레지스트리가
  생성하는 RESOURCE_UNION 으로 교체. 신규 리소스 타입은 서술자 등록만으로 불변식 검사에
  자동 편입되고, 미등록 타입 Grant 는 신설 RI-9(고아 모듈 권한)가 SQL 오류 없이 보고
- **소유자 정리 훅** (Phase 3 WP-K2) — 회원 삭제 시 각 리소스 타입이 자기 정리를
  서술한다. 파일만 정리되고 **도메인은 남던 기존 결함 해소.** 삭제 트랜잭션은 O(1)
  (표식+잡 큐잉)이고 실제 정리는 퍼지 워커가 배치로 수행하되, 은닉은 표식이 즉시
  담당해 가시성 창이 없다(DEC-3). 퍼지 실패는 재시도 큐 + RI-10 순찰이 검출
- 관리자 콘솔 화면: 온보딩·내 계정·파일·도메인·감사 로그·시뮬레이터·거버넌스·버전
- SMTP 메일 발송 어댑터 (§13.2 결정 — 개발 단계 Gmail SMTP). `MAIL_TRANSPORT=smtp` 로 전환
- 탈퇴 회원 개인정보 파기 배치 (§13.2 결정 — 30일 후 익명화). 계정 행은 남겨 감사 추적을 보존
- **시스템 설정 화면** — 접속 정보(SMTP·파일 저장소)를 관리 화면에서 주입한다.
  환경 변수로 남는 것은 부트스트랩 3종(`DATABASE_URL`·`SETTINGS_ENCRYPTION_KEY`·`JWT_SECRET`)뿐
- 이메일(로그인 식별자) 변경 (MEM-1) — 재인증 + 새 주소 소유 확인 + 옛 주소 통지
- 버전·시스템 상태 조회 API (`GET /admin/version`)

### Fixed
- API 가 `.env` 를 읽지 않아 기동 자체가 실패하던 문제 (`pnpm start:dev` 최초 동작)
- CORS 부재로 브라우저의 모든 API 호출이 차단되던 문제
- 개발 메일러가 본문을 남기지 않아 로컬에서 확인 토큰을 얻을 수 없던 문제

### Changed
- G-2 금지 패턴 검사를 프론트엔드로 확대 — 화면에서 권한을 재구현하는 것을 차단
- 개발 서버가 파일 변경 시 자동 재시작한다 (`ts-node` → `ts-node-dev`).
  라우트를 추가하고도 재기동을 잊어 404 를 원인으로 오해하던 상황을 없앤다
- **접속 정보 주입 원칙 변경**: "환경 변수로만"(WP-9) → "부트스트랩 3종을 뺀 나머지는 관리 화면".
  범용 제품이라 내려받은 사람이 배포 파일을 고치지 않고 자기 환경을 넣을 수 있어야 한다

## [0.2.0] - 2026-08-08

Phase 2 완료 — 파일·도메인 리소스 권한과 런타임 거버넌스.

### Added
- 파일: 업로드·다운로드(서명 URL)·목록·수정·삭제·공유 (FILE-1~7)
- 도메인: 등록·DNS 검증·조회·수정·삭제·운영 위임·소유자 이전 (DOM-1~7)
- 감사 로그 조회 (ADM-4)·권한 시뮬레이터 (ADM-5)·거버넌스 콘솔 API
- 런타임 불변식 순찰 RI-1~RI-8 (10분 주기, L-1 자동 회수·L-2 동결·L-3 보고)
- 감사 로그 해시 체크포인트, 이상 탐지 AN-1~4
- G-1 매트릭스에 **관계 축** 신설 — 같은 역할이라도 소유·공유·만료·차단에 따라 갈리는
  판정을 250셀로 고정
- 롤백 절차서·역방향 SQL·L-1 복구 스크립트

### Fixed
- TOTP 재등록에 재인증이 없어 세션 탈취자가 2차 인증기를 교체할 수 있던 결함 (CR-1)
- 인증용 속도 제한이 전 라우트에 적용되어 정상 사용자가 6회 호출에 429 를 받던 문제
- 입력 화이트리스트(ValidationPipe)가 부트스트랩에만 등록되어 어떤 테스트도 지나지 않던 문제

### Changed
- Grant 회수 시 회수 전 행 **전체**를 감사에 남긴다 (복구 가능성 확보)
- 도메인 FQDN 유일성을 부분 유니크로 교체 — 삭제된 도메인의 재등록이 가능해졌다

## [0.1.0] - 2026-08-07

Phase 1 완료 — 인증·권한 평가기·감사·회원/역할 관리.

### Added
- 인증 (AUTH-1~4): 가입·로그인·토큰 회전·비밀번호 재설정, 온보딩 게이트
- 권한 평가기 (§4.7 0~5단계)와 Guard 체계, 권한 스냅샷 캐시
- 감사 로그: 트랜잭션 내 기록, 월 파티션, append-only 강제
- 회원 관리 (MEM-1~6)·관리자 콘솔 (ADM-1~3)·우위 검사
- 거버넌스 게이트 5종 (G-1 매트릭스 · G-2 금지 패턴 · G-3 공격 시나리오 ·
  G-4 시드 정합성 · G-5 미선언 엔드포인트)
- break-glass 비상 회수 절차서

[Unreleased]: https://github.com/jnano/stonex/compare/v0.2.0...HEAD
[0.2.0]: https://github.com/jnano/stonex/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/jnano/stonex/releases/tag/v0.1.0
