# 런타임 불변식 (RI-1 ~ RI-7)

기획서 §14.3의 불변식을 SQL 파일로 선언 관리하는 위치. **Phase 2(런타임 순찰 워커 도입)에서 작성한다.**

파일 규칙: `ri-<번호>-<슬러그>.sql` (예: `ri-1-active-super-admin.sql`). 각 파일은 "위반 행을 반환하는 SELECT" 형태로 작성하며, 순찰 워커는 결과가 0행이 아니면 위반으로 처리한다.
